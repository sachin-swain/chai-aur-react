export { TodoContext, TodoProvider, useTodo } from "./TodoContext";

// The bundler (like Vite, Webpack) looks for index.js (or index.jsx) by default inside the contexts folder.
// import {TodoProvider} from './contexts'
